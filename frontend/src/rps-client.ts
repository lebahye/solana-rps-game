import { PublicKey } from '@solana/web3.js';

export class RPSGameClient {
  constructor(
    publicKey: PublicKey,
    programId: PublicKey
  ) {
    // Initialize your game client here
  }

  // Add your game client methods here
}